const env = require("./env");
const config = {
    db:{
        host: "localhost",
        user:"armando",
        password:"12345678",
        database:"dbmechanics"
    }
};

module.exports = config